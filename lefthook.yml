pre-commit:
  jobs:
    - name: check
      run: pnpm check:staged
      stage_fixed: true

pre-push:
  jobs:
    - name: typecheck
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}