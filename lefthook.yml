pre-commit:
  jobs:
    - name: check
      run: pnpm check:staged
      stage_fixed: true

pre-push:
  jobs:
    - name: typecheck
      run: pnpm typecheck
    - name: check-exports
      run: pnpm check:exports

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}